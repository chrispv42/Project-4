{
  "name": "ol-time-muscle",
  "private": true,
  "scripts": {
    "dev": "npm run dev",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client start",
    "dev:db": "npm --prefix server run db:test",
    "install:all": "npm --prefix server install && npm --prefix client install"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.4",
    "jsonwebtoken": "^9.0.3"
  }
}
